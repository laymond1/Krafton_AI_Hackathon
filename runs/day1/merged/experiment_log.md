# Experiment Log

## Metadata

- Day: day1
- Controller: onesun
- Goal: Build execution scaffold and select the first low-risk model path for Day 1 multiplication
- Date: 2026-03-28

## Usage

- One row per meaningful test, prompt iteration, or decision-driving experiment.
- Keep entries short and factual.
- Prefer evidence over interpretation.

| Time | Owner | Hypothesis or Question | Action Taken | Result | Decision Impact | Next Step |
| --- | --- | --- | --- | --- | --- | --- |
| 13:41 | Codex | Parser outputs are enough to start Builder without waiting for more strategy docs | Reviewed `README.md`, raw problem, both parser outputs, and current merged templates | Confirmed Builder should produce ordered execution plan plus draft artifacts | Proceed with Builder-first scaffold instead of abstract planning | Write `runs/day1/codex/builder.md` and a single-file Python draft |
| 13:41 | Codex | A single-file `Problem 1-2` scaffold reduces late packaging risk | Created `runs/day1/codex/submission_draft.py` with encoding, model scaffold, decode, eval, and parameter counting hooks | Working draft exists for Verifier/Writer handoff | Current chosen direction is now concrete and comparable | Run smoke test in a PyTorch-capable environment |
| 13:41 | Codex | Local environment may already support PyTorch smoke tests | Checked `python3` and attempted `import torch` | Python 3.9.6 is present, but `torch` is not installed | Execution is currently blocked at runtime, not at planning or file structure | Activate/install a PyTorch environment, then run smoke test and first sweep |
| 13:47 | Codex | `.venv` with torch should unblock real scaffold verification | Ran `.venv/bin/python runs/day1/codex/submission_draft.py --smoke-test` | Smoke test passed; worked examples and default parameter count `100226` printed correctly | Builder scaffold is now runtime-validated, not only drafted | Run a short training sanity check and then candidate sweeps |
| 13:48 | Codex | The draft training loop may still fail even if smoke tests pass | Ran `.venv/bin/python runs/day1/codex/submission_draft.py --demo-train --epochs 2 --train-size 1024 --eval-size 128 --batch-size 128` | Training loop completed; exact-match stayed at `0.0234` after 2 epochs on the sanity run | File is usable for experiments, but results are still baseline-level only | Add candidate sweep support and begin broader search |
| 13:50 | Codex | A tiny candidate sweep can identify the first sensible size to scale up | Ran `--sweep-candidates --epochs 1 --train-size 256 --eval-size 32 --batch-size 64` in `.venv` | Candidate 1 (`17218` params) and candidate 3 (`100226` params) each reached `0.0625`; candidate 1 is far cheaper | Candidate 1 is the best current long-run starting point | Run a longer experiment on candidate 1 before changing architecture |
| 13:53 | Codex | Candidate 1 may stay efficient after a longer run | Ran `--demo-train --candidate-index 1 --epochs 10 --train-size 4096 --eval-size 256 --batch-size 256` in `.venv` | Loss dropped to `0.6474`, but exact-match stayed around `0.0195` | Cheap model is probably too weak or too slow-to-learn for the current path | Test a larger model before spending more time on candidate 1 |
| 13:56 | Codex | A larger model may recover exact-match if size is the main issue | Ran `--demo-train --candidate-index 3 --epochs 10 --train-size 4096 --eval-size 256 --batch-size 256` in `.venv` | Larger model reached lower loss `0.6310`, but exact-match still stayed at `0.0195` | Model size alone is not fixing the current gap | Add a bit-accuracy diagnostic before choosing the next architecture change |
| 13:58 | Codex | Low exact-match may still hide partial bit learning | Added `bit_accuracy()` and ran a 1-epoch candidate-1 check | Candidate 1 showed `0.5365` bit accuracy with `0.0000` exact-match | Current path is learning some output structure, but not enough for sequence correctness | Investigate decode/training alignment and output-bit error patterns |
| 14:05 | Codex | `AdderBoard` top solutions may offer a much better tiny-transformer prior than the generic decoder baseline | Read `AdderBoard` prize files and rebuilt the multiplier baseline around tied embedding, RoPE, RMSNorm, and tiny SwiGLU blocks | New default scaffold shrank from `100226` params to `182` params while keeping the same generic decode path | The search space is now much closer to known small arithmetic transformers | Re-run smoke tests and fresh candidate sweeps with the new family |
| 14:06 | Codex | The `AdderBoard`-inspired family should show better early efficiency | Ran new `--sweep-candidates --epochs 1 --train-size 256 --eval-size 64 --batch-size 64` | Candidate 3 (`182` params) reached `0.0469` exact-match; candidate 4 (`254` params) reached `0.5794` bit accuracy | New family dominates the old baseline on parameter efficiency and early bit learning | Run a longer candidate-3 experiment with larger data |
| 14:07 | Codex | Short-run improvement may survive under larger sampled training sets | Ran candidate 3 for 3 epochs with `100000` sampled pairs | Exact-match stayed at `0.0254`, but bit accuracy improved to `0.6296` | Current family learns bits better than before, yet still misses sequence-level correctness | Extend the run to 10 epochs before deciding on a deeper redesign |
| 14:08 | Codex | Candidate 3 may still open up later with more epochs on the larger sampled set | Ran candidate 3 for 10 epochs with `100000` sampled pairs | Exact-match stayed around `0.0254`; bit accuracy stabilized near `0.6273` | The `AdderBoard`-inspired tiny family is a stronger baseline, but still far from the target under current settings | Inspect per-bit failures or shift architecture/training strategy |
| 14:15 | Codex | Top-5 Hand-Coded and Top-5 Trained leaderboard ideas can be turned into explicit candidate families | Added `quadratic`, `arc`, `phase_tied`, `k_rot_q`, `k_eq_v`, tied-`O=Q^T`, shared-RMSNorm, `relu2`, and `swiglu` options to the scaffold | New default candidate dropped to `39` params while keeping generic decoding intact | The scaffold now directly reflects leaderboard design patterns instead of a single borrowed family | Run a fresh sweep across the mixed idea set |
| 14:16 | Codex | The mixed idea set should expose which leaderboard patterns transfer best to multiplication | Ran fresh `--sweep-candidates --epochs 1 --train-size 256 --eval-size 64 --batch-size 64` | Best early exact-match came from candidate 3 (`51` params, arc + `K=V`), best bit accuracy from candidate 5 (`142` params) | Trained top-5 ideas transfer better than the hand-inspired sparse family so far | Compare a longer candidate-3 run against a slightly larger control |
| 14:17 | Codex | `51`-param trained-inspired candidate may be the best tiny baseline | Ran candidate 3 for 3 epochs with `100000` sampled pairs | Exact-match rose to `0.0254`, bit accuracy to `0.6144` | Candidate 3 is efficient but still not near qualification | Keep as the current tiny baseline |
| 14:17 | Codex | Slightly larger learned-embed control may outperform the tiny arc family | Ran candidate 5 for 3 epochs with `100000` sampled pairs | Candidate 5 matched exact-match `0.0254` and reached `0.6219` bit accuracy at `142` params | Extra width helps bit learning a little, but not enough to change the overall conclusion | Next decision should come from error analysis, not blind scaling |
| 14:24 | Codex | Per-bit analysis can reveal whether exact-match is blocked by specific positions or by a global collapse | Added `--analyze-bits`, per-bit accuracy, false-positive/false-negative rates, and `first_wrong_bit` reporting to the scaffold | Analysis tooling now supports position-wise diagnosis on random or all-pairs evaluation | The next iterations can target concrete failure modes instead of broad guessing | Run all-pairs bit analysis on the best current candidates |
| 14:25 | Codex | Candidate 3 may be failing due to a specific output band | Ran all-pairs bit analysis on candidate 3 after 3 epochs / 100k sampled pairs | Candidate 3 predicts essentially all zeros: every bit has `pred1=0.0000`, only false negatives, and first wrong bits concentrate from `P00` through `P05` | The tiny arc family is trapped in a majority-class solution, not a nuanced sequence error mode | Compare against candidate 5 to see if the collapse is family-specific |
| 14:26 | Codex | Candidate 5 may escape the all-zero trap even if exact-match remains low | Ran all-pairs bit analysis on candidate 5 after 3 epochs / 100k sampled pairs | Candidate 5 predicts some ones, but underpredicts them across almost every bit; the heaviest weakness is the middle band `P02` to `P08`, and earliest failures are still dominated by low bits | Current best control is not collapsed, but it still lacks enough positive-bit confidence to solve multiplication | Next design step should directly address low-bit and mid-bit one-rate underproduction |
| 14:31 | Codex | Underprediction may be reduced by giving candidate 5 more output-calibration freedom | Added candidate-5-derived variants with untied output head, unshared norm, and optional fixed positional encoding | Candidate family now directly tests whether output calibration, not width, is the main blocker | Compare new variants against the candidate-5 baseline with the same bit-analysis harness | Run all-pairs bit analysis on candidate 6 and candidate 7 |
| 14:33 | Codex | Untying the output head and norms may lift one-rate without changing the training protocol | Ran all-pairs bit analysis on candidate 6 (`158` params) for 3 epochs / 100k sampled pairs | Candidate 6 improved bit accuracy to `0.6353` and raised `pred1` across most positions compared with candidate 5 | This is the first concrete architectural fix for the underprediction failure mode | Keep candidate 6 and test whether the improvement is stable over longer training |
| 14:34 | Codex | Adding fixed positional encoding may further improve candidate 6 | Ran all-pairs bit analysis on candidate 7 (`158` params, fixed PE) for 3 epochs / 100k sampled pairs | Candidate 7 became less stable: several bits still collapsed toward zero, while `P03` overpredicted ones | Fixed PE did not help this family under current settings | Drop candidate 7 and continue with candidate 6 |
| 14:37 | Codex | Candidate 6 improvement may vanish under longer training | Ran all-pairs bit analysis on candidate 6 for 10 epochs / 100k sampled pairs | Improvement held: exact-match stayed around `0.0317`, bit accuracy rose to `0.6380`, and low-bit/mid-bit one-rate remained the main gap | Candidate 6 is now the strongest current control and a better base for targeted fixes | Focus next changes on `P02` to `P08` one-rate recovery |
| 15:03 | Codex | Claude independent run hit session-usage limits and must be absorbed into the Codex thread | Reviewed `runs/day1/claude/strategist.md` and `runs/day1/claude/critic.md`, then aligned their conclusions with the active Codex path | Claude findings are now captured as input constraints rather than a parallel execution branch | Prevents split ownership and keeps one coherent experiment trail | Update merged docs and operator log to show Codex now owns the integrated path |
| 15:40 | Codex | The repo should stop defaulting to the missing `.venv` and use the maintained conda env instead | Verified `conda env list` and switched base guidance to `conda run -n wslim python ...` | `wslim` is the canonical runtime path for this repo | Future experiments should use one consistent command path | Run Day 1 diagnostics with `conda run -n wslim python runs/day1/codex/submission_draft.py ...` |

## Summary

- Best signal found: candidate 6 partially fixes candidate 5's underprediction problem without changing the required training protocol, but the remaining weakness is still concentrated in low and middle output bits.
- Failed path to avoid: letting `Problem 1-1` proof work block all usable execution artifacts.
- Follow-up needed: design the next iteration specifically to raise low-bit and mid-bit one prediction rates for candidate 6, with Claude strategist/critic notes treated as inherited context rather than a separate workstream.
